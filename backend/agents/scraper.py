# TODO: Scraper Agent
# Responsibility: Pull job listings from LinkedIn, Indeed, Greenhouse, Lever.
# Writes raw job data to the `jobs` table. Never applies or scores — just scrapes.
# Entry point: run()
