# TODO: Orchestrator Agent
# Responsibility: Read user config, trigger other agents in the right order,
# track overall workflow state in PostgreSQL/Redis.
# This agent does NOT do scraping, matching, or applying — it only coordinates.
