# TODO: Resume Match Agent
# Responsibility: Score each scraped job (0–100) against the user's resume.
# Uses Claude API to reason about skill overlap, seniority fit, etc.
# Updates `match_score` field on Job records.
# Entry point: run()
