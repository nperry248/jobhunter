# TODO: Redis + Celery setup
# Defines the Celery app instance and task queue configuration.
# Celery is the task runner that lets agents run in the background on a schedule
# without blocking the FastAPI web server.
