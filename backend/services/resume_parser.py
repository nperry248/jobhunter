# TODO: Resume Parser service
# Responsibility: Accept a PDF file path, extract raw text, return structured data.
# Used by: Resume Match Agent
# This is a "service" (not an agent) because it's a pure utility function —
# it doesn't interact with the DB or queue on its own.
