# ─────────────────────────────────────────────────────────────
# .env.example — Template for environment variables
#
# HOW TO USE:
#   1. Copy this file: `cp .env.example .env`
#   2. Fill in the real values in .env
#   3. NEVER commit .env — it's in .gitignore
#   4. .env.example IS safe to commit (no real secrets here)
# ─────────────────────────────────────────────────────────────


# ── AI ──────────────────────────────────────────────────────
# Your Anthropic API key. Get it at: https://console.anthropic.com
# Used by: Resume Match Agent, Outreach Agent
ANTHROPIC_API_KEY=sk-ant-your-key-here


# ── Database ─────────────────────────────────────────────────
# PostgreSQL connection string. Format: postgresql+asyncpg://user:pass@host:port/dbname
# The `+asyncpg` tells SQLAlchemy to use the async driver (required for FastAPI).
# For local Docker: use the values from docker-compose.yml
DATABASE_URL=postgresql+asyncpg://jobhunter:jobhunter@localhost:5432/jobhunter

# A separate DB for running tests — NEVER point this at your real dev DB.
# Tests create and drop tables freely, which would destroy real data.
TEST_DATABASE_URL=postgresql+asyncpg://jobhunter:jobhunter@localhost:5432/jobhunter_test


# ── Redis / Task Queue ────────────────────────────────────────
# Redis connection string. Used by Celery as the message broker.
# Database 0 = default; database 1 = reserved for Celery results backend
REDIS_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1


# ── API Server ────────────────────────────────────────────────
# The host/port the FastAPI server binds to.
# BACKEND_CORS_ORIGINS is a comma-separated list of allowed frontend origins.
# This prevents random websites from making API requests to your backend.
API_HOST=0.0.0.0
API_PORT=8000
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000


# ── Agent Behavior ────────────────────────────────────────────
# How many jobs to scrape per run (prevents runaway scraping)
SCRAPER_MAX_JOBS_PER_RUN=100

# Minimum score (0–100) a job must have to be auto-applied to
MATCH_SCORE_THRESHOLD=70

# Max retries for any external HTTP call before giving up
MAX_RETRY_ATTEMPTS=3

# Base delay (seconds) for exponential backoff between retries
# Retry 1 waits 1s, retry 2 waits 2s, retry 3 waits 4s (doubles each time)
RETRY_BASE_DELAY=1.0

# How often (in seconds) the scraper runs. 3600 = once per hour.
SCRAPER_INTERVAL_SECONDS=3600

# Timeout in seconds for each outbound HTTP request to a job board API.
SCRAPER_REQUEST_TIMEOUT=30

# Seconds to pause between consecutive Claude API calls in the Resume Match agent.
# Prevents hitting Claude's rate limiter when scoring many jobs in a single run.
# Set lower (or 0.0) if you have a high-tier API plan with more RPM headroom.
CLAUDE_REQUEST_DELAY_SECONDS=0.5


# ── Scraper — Company Slugs ───────────────────────────────────
# JSON dict of {slug: "Human Name"} pairs for each platform.
# The slug is the company identifier in the job board URL.
#
# Finding a slug:
#   Greenhouse: https://boards.greenhouse.io/{slug}
#   Lever:      https://jobs.lever.co/{slug}
#
# Add/remove companies here without touching any Python code.
GREENHOUSE_SLUGS={"airbnb": "Airbnb", "figma": "Figma", "ramp": "Ramp", "coinbase": "Coinbase", "plaid": "Plaid"}
LEVER_SLUGS={"notion": "Notion"}


# ── Apply Agent ───────────────────────────────────────────────
# Run Playwright browser headlessly (no visible window).
# Set to false when debugging to watch the browser fill forms in real time.
APPLY_HEADLESS=true

# Fill forms and screenshot but NEVER click submit.
# Use this when verifying the agent works before enabling real submissions.
APPLY_DRY_RUN=false

# Only auto-apply to jobs scoring at or above this threshold (0–100).
APPLY_MIN_SCORE=70

# Where to save Playwright screenshots (form + result page per job).
# The directory is created automatically if it doesn't exist.
SCREENSHOTS_DIR=data/screenshots

# Handoff mode: agent fills standard fields in a VISIBLE browser, then pauses
# so you can complete custom questions / dropdowns and click Submit yourself.
# The job stays as REVIEWED in the DB until you confirm it was submitted.
# When true, APPLY_HEADLESS is automatically overridden to false.
APPLY_HANDOFF=false

# How long (seconds) to keep the browser open per job in handoff mode.
# Default 300 = 5 minutes. Increase if forms are complex.
APPLY_HANDOFF_WAIT_SECONDS=300


# ── Orchestrator Agent ────────────────────────────────────────
# Which Claude model the Orchestrator uses for tool-use decisions.
# Haiku is fast and cheap — swap to claude-sonnet-4-6 for harder multi-step tasks.
ORCHESTRATOR_MODEL=claude-haiku-4-5-20251001

# Hard cap on tool calls per session. Prevents infinite loops and runaway costs.
ORCHESTRATOR_MAX_TURNS=10

# Max tokens per Claude response.
ORCHESTRATOR_MAX_TOKENS=4096

# If true, the Orchestrator won't fire real agents (scraper, scorer, apply).
# Tools return mock data. Safe for testing the agent loop without side effects.
ORCHESTRATOR_DRY_RUN=false

# ── Logging ───────────────────────────────────────────────────
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
